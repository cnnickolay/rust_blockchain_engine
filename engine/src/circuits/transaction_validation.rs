use anyhow::Result;
use protocol::request::{Response, Request};

use crate::{configuration::ValidatorReference, blockchain::blockchain::BlockChain};

// pub fn process_request(blockchain: &mut BlockChain) -> Result<(Response, Vec<(ValidatorReference, Request)>)> {
    
// }